# Блок 2. Базовые полётные команды. (Задания)

## `ARM`/`DISARM`

Дрон стоит на столе. Запускаем моторы (`ARM`) через 5 секунд выключаем моторы (`DISARM`).

## `TAKEOFF`/`LAND`

Взлететь (`ARM`, `TAKEOFF`), зависнуть в воздухе на 5 секунд, а потом сесть (`LAND`).
